export {DEFAULT_RUN_TIMEOUT_MS} from '@shipfox/api-definitions-dto';

/** The largest count prefix the listener drain plans before byte packing. */
export const LISTENER_BATCH_PLATFORM_EVENT_COUNT_LIMIT = 100;
