name: Simple build

triggers:
  on_demand:
    source: manual
    event: fire
  on_push:
    source: github
    event: push
    on: main
    filter: event.ref == "refs/heads/main"

jobs:
  build:
    steps:
      - run: npm install
      - run: npm run build
        name: build_output
