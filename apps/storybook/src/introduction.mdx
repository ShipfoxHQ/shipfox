import {storybookLinks} from '../preview-manifest.js';

<div className="shipfox-introduction">
  <div className="shipfox-introduction__content">
    <p className="shipfox-introduction__eyebrow">Shipfox component library</p>
    <h1 className="shipfox-introduction__title">Browse the components behind Shipfox.</h1>
    <div className="shipfox-introduction__summary">Choose a package to explore its components, states, and interaction patterns. Use the theme control above to review every surface in light or dark mode.</div>

    <h2 className="shipfox-introduction__section-title">Choose a package</h2>
    <div className="shipfox-introduction__links">
      {storybookLinks.map((storybook) => (
        <a className="shipfox-introduction__link" href={storybook.url} key={storybook.id}>
          <span className="shipfox-introduction__link-title">{storybook.title}</span>
          <span className="shipfox-introduction__link-action">Browse stories →</span>
        </a>
      ))}
    </div>
  </div>
</div>
