---
title: "Sentry integration"
description: "Connect Sentry to trigger workflows from issue lifecycle events."
catalog:
  name: "Sentry"
  summary: "Receive Sentry issue events and route error signals into Shipfox workflows."
  capabilities: ["events"]
  categories: ["observability"]
  aliases: ["errors", "monitoring", "crash"]
  icon: "sentry"
---

## Authentication

Shipfox connects through a Sentry App. Install the App in the target Sentry
organization and authorize its requested access.

## Integration connection identity

The slug of a Sentry integration connection defaults to `sentry_<organization>`, such
as `sentry_acme`. Use the slug of the workspace integration connection as a workflow
trigger `source`.

```yaml
triggers:
  on_issue:
    source: sentry_acme
    event: issue.created
```

For trigger field rules, see the [workflow schema reference](/reference/workflow-schema#trigger-fields).

## Capabilities

| Capability | Details |
| --- | --- |
| Events | [View Sentry events](/integrations/sentry/events). |

[Connect Sentry](/integrations/sentry/setup) to create an integration connection.
