---
title: "Custom webhook integration"
description: "Create an HTTP endpoint that triggers workflows from requests sent by any system."
catalog:
  name: "Custom webhook"
  summary: "Receive events from any system through a unique HTTP endpoint."
  capabilities: ["events"]
  categories: ["custom"]
  aliases: ["http", "generic"]
  icon: "webhooks"
---

## Authentication

Custom webhooks do not use a provider account. The generated ingest URL is the
credential. Keep it secret because anyone with the URL can send events.

## Integration connection identity

Choose a unique integration connection slug in the workspace, such as
`deploy_hook`. Use the slug as a workflow trigger `source`.

```yaml
triggers:
  on_webhook:
    source: deploy_hook
```

For trigger field rules, see the [workflow schema reference](/reference/workflow-schema#trigger-fields).

## Capabilities

| Capability | Details |
| --- | --- |
| Events | [View custom webhook events](/integrations/webhooks/events). |

[Create a custom webhook](/integrations/webhooks/setup) to create an integration connection.
