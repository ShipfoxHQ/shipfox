---
title: "Workflow YAML reference"
sidebarTitle: "Workflow Schema"
description: "Define when Shipfox runs and what each workflow does."
full: true
tableOfContent: true
---

import {
  AgentIntegrationFields,
  AgentSessionFields,
  AgentStepFields,
  CheckoutFields,
  CheckoutPermissionsFields,
  CheckoutStepFields,
  ConcurrencyFields,
  EnvironmentVariables,
  GateFailureFields,
  GateFields,
  JobCheckoutFields,
  JobFields,
  ListeningBatchFields,
  ListeningFields,
  RunStepFields,
  StepFields,
  StepOutputs,
  ToolStepFields,
  ToolStepOutputs,
  TopLevelFields,
  TriggerFields,
} from '../../generated/reference/workflow-schema.mdx';

Use this page to find the fields you can add to a workflow YAML file. Each
section explains one part of the file and highlights it in an example.

<Callout title="Enable editor autocomplete" type="info">
  Add `# yaml-language-server: $schema=https://www.shipfox.io/docs/workflow.schema.json`
  as the first line of a workflow file.
</Callout>

## Workflow [#top-level-fields]

<TopLevelFields />

## `triggers.<trigger_id>` [#trigger-fields]

<TriggerFields />

## `concurrency` [#concurrency-fields]

<ConcurrencyFields />

## `env` [#environment-variables]

<EnvironmentVariables />

## `jobs.<job_id>` [#job-fields]

<JobFields />

### `jobs.<job_id>.checkout` [#job-checkout-fields]

<JobCheckoutFields />

### `jobs.<job_id>.listening` [#listening-fields]

<ListeningFields />

#### `jobs.<job_id>.listening.batch` [#listening-batch-fields]

<ListeningBatchFields />

### `jobs.<job_id>.steps[*]` [#step-fields]

<StepFields />

#### `steps[*].gate` [#gate-fields]

<GateFields />

#### `steps[*].gate.on_failure` [#gate-failure-fields]

<GateFailureFields />

#### `steps[*].outputs` [#step-outputs]

<StepOutputs />

### `steps[*]` run step [#run-step-fields]

Accepts the shared step fields plus:

<RunStepFields />

### `steps[*]` agent step [#agent-step-fields]

Accepts the shared step fields plus:

<AgentStepFields />

#### `steps[*].integrations[*]` [#agent-integration-fields]

<AgentIntegrationFields />

#### `steps[*].session` [#agent-session-fields]

<AgentSessionFields />

### `steps[*]` tool step [#tool-step-fields]

<ToolStepFields />

#### `steps[*].outputs` tool step [#tool-step-outputs]

<ToolStepOutputs />

### `steps[*]` checkout step [#checkout-step-fields]

Accepts the shared step fields plus:

<CheckoutStepFields />

#### `steps[*].checkout` [#checkout-fields]

<CheckoutFields />

#### `steps[*].checkout.permissions` [#checkout-permissions-fields]

<CheckoutPermissionsFields />

## Related pages

<Cards>
  <Card title="Contexts" href="/reference/contexts">
    Every context, its properties, and the fields that can read it.
  </Card>
  <Card title="Expressions" href="/reference/expressions">
    The syntax, operators, and functions available inside an expression.
  </Card>
  <Card title="Model Providers" href="/reference/model-providers">
    Every provider ID, default model, and how agent config resolves.
  </Card>
  <Card title="Limits" href="/reference/limits">
    Every default and cap: attempts, timeouts, log budgets, env sizes.
  </Card>
</Cards>
