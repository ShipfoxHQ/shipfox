---
"@shipfox/client-shell": patch
"@shipfox/client-workspace-settings": patch
---

Validates the published default client composition from a clean external Vite consumer and fixes typed access to the events settings route.
