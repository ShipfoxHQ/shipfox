---
"@shipfox/workflow-templates": minor
---

The ticket to pull request template's `label` trigger also starts a run when a Linear issue is created with the chosen label. The template revision is now 2.
