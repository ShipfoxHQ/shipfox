---
"@shipfox/api-agent-dto": major
"@shipfox/api-agent": major
---

Removes the obsolete `claude.auth_token` field from the managed runtime contract and uses the shared API credential for Claude authentication.
