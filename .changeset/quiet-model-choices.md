---
"@shipfox/workflow-templates": major
"@shipfox/api-agent-access-dto": major
"@shipfox/api-agent-access": minor
---

Replaces model profiles and `resolved_models` with measured model and thinking suggestions, removing the public resolver and response field before guided setup launches.
