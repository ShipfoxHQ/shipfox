---
"@shipfox/react-ui": minor
"@shipfox/client-logs": patch
---

Adds a `tone="contrast"` option to `Markdown` for content on the dark code surface. Agent prompts and replies in logs now use it, so their text stays readable in light mode.
