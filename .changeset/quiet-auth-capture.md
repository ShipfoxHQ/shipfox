---
"@shipfox/client-auth": patch
---

Capture the `@shipfox/client-auth` workspace switcher Storybook stories as Argos visual snapshots. Vitest now runs a browser `storybook` project that screenshots the switcher states in light and dark and uploads them to the `client-auth-storybook` Argos build in CI.
