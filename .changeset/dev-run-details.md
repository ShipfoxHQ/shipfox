---
"@shipfox/api-triggers-dto": minor
"@shipfox/api-triggers": minor
---

Accepts local workflow `content` with an optional `ref`, and returns the resolved `ref` and validation `warnings`.
Adds `availableTriggerKeys` and replay-event mismatch fields to refusal details.
