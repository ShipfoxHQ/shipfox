---
"@shipfox/client-agent": minor
---

Add a workflow example usage modal for configured model providers, with model selection, ready-to-copy workflow YAML, model id browsing, and automatic opening after first configuration.
