---
"@shipfox/api-agent": patch
"@shipfox/api-workflows": patch
---

Moves agent model provider credentials onto the shared secrets store while keeping provider config metadata and runtime resolution behavior intact.
