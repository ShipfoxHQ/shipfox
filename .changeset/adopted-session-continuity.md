---
"@shipfox/client-shell": minor
"@shipfox/client-api": minor
---

Restores adopted sessions at boot, supports bounded same-principal token continuation and renewal, gates requests until renewal succeeds or the session ends, invokes each release reason once, and publishes the client-api and client-shell hooks as one compatible release train.
