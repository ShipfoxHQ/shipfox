---
"@shipfox/api-runners-dto": minor
---

Adds a credential-free, nullable checkout intent to the runner job payload and a new checkout-token response DTO (basic/bearer auth with a validated expiry), establishing the contract for runner-managed repository checkouts.
