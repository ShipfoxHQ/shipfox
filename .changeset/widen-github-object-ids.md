---
"@shipfox/api-integration-github": minor
---

Adds 64-character Git object ID support to `create_branch` and rejects all-zero expected head object IDs in `create_commit` through the shared validator.
