name: Failing step
runner: e2e
triggers:
  on_push:
    source: gitea
    event: push
    filter: 'event.ref == "refs/heads/main"'
jobs:
  build:
    steps:
      - key: boom
        run: |
          echo "about to fail"
          exit 1
